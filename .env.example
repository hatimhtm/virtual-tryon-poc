# ─────────────────────────────────────────────────────────────────────────────
# Atelier Virtuel — required env vars
# Copy to `.env.local` and fill in. Never commit the real values.
# ─────────────────────────────────────────────────────────────────────────────

# Google Generative AI key — get one at https://aistudio.google.com/apikey
# Used by both /api/validate-image (Gemini 2.5 Flash) and
# /api/generate-tryon (Gemini 3.1 Flash Image preview).
GOOGLE_GENAI_API_KEY=
